{
  "name": "formulario-freecodecamp-src",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix .. run dev",
    "build": "npm --prefix .. run build",
    "preview": "npm --prefix .. run preview",
    "test": "npm --prefix .. test",
    "lint": "npm --prefix .. run lint",
    "typecheck": "npm --prefix .. run typecheck"
  }
}
